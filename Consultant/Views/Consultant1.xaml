<Page
    x:Class="Consultant.Views.Consultant1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Consultant.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="White">

    <SplitView DisplayMode="CompactOverlay">
        <SplitView.Pane>
            <Grid Background="DarkRed">
                <Image Source="../MenuBar.jpeg" HorizontalAlignment="Left" Width="50"/>
                <ListView x:Name="ListPane" ItemsSource="{Binding Path=Items}" Background="Transparent" SelectionChanged="ListPane_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Path=Image}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </SplitView.Pane>
        <SplitView.Content>
            <Frame>
                <Image Source="../ScotiaBackground.jpeg" Stretch="Fill"/>
            </Frame>
        </SplitView.Content>
    </SplitView>
</Page>
